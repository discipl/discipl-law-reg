
const core = require('discipl-core')

/*
 * publishes the FLINT model (as JSON) in linked verifiable claims
 * in the channel of the given ssid
 * returns a list with links to start acts
 */
const publish = async (ssid, flint_model) => {

}

const get = async (case, did) => {

}

const observe = async (case, did) => {

}

const take = (ssid, case, act, obj = null) => {

}

module.exports = {
  publish,
  get,
  observe,
  take
}
